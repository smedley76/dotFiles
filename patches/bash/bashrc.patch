--- .bashrc	2022-01-14 11:52:14.000000000 -0600
+++ also.bashrc	2022-09-28 10:26:33.546981045 -0500
@@ -90,11 +90,11 @@
 
 unset use_color safe_term match_lhs sh
 
-#alias cp="cp -i"                          # confirm before overwriting something
-#alias df='df -h'                          # human-readable sizes
-#alias free='free -m'                      # show sizes in MB
-#alias np='nano -w PKGBUILD'
-#alias more=less
+alias cp="cp -i"                          # confirm before overwriting something
+alias df='df -h'                          # human-readable sizes
+alias free='free -m'                      # show sizes in MB
+alias np='nano -w PKGBUILD'
+alias more=less
 
 xhost +local:root > /dev/null 2>&1
 
@@ -118,10 +118,12 @@
 {
   if [ -f $1 ] ; then
     case $1 in
+      *.tar.xz)     tar xf $1   ;;
+      *.tar.zst)   unzstd $1    ;;
       *.tar.bz2)   tar xjf $1   ;;
       *.tar.gz)    tar xzf $1   ;;
       *.bz2)       bunzip2 $1   ;;
-      *.rar)       unrar x $1     ;;
+      *.rar)       unrar x $1   ;;
       *.gz)        gunzip $1    ;;
       *.tar)       tar xf $1    ;;
       *.tbz2)      tar xjf $1   ;;
@@ -135,3 +137,4 @@
     echo "'$1' is not a valid file"
   fi
 }
+export GPG_TTY=$(tty)
