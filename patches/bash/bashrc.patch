--- bashrc.orig	2022-09-28 12:18:41.530989695 -0500
+++ bashrc.new	2022-09-28 12:28:11.233387971 -0500
@@ -90,11 +90,11 @@
 
 unset use_color safe_term match_lhs sh
 
-#alias cp="cp -i"                          # confirm before overwriting something
-#alias df='df -h'                          # human-readable sizes
-#alias free='free -m'                      # show sizes in MB
-#alias np='nano -w PKGBUILD'
-#alias more=less
+alias cp="cp -i"                          # confirm before overwriting something
+alias df='df -h'                          # human-readable sizes
+alias free='free -m'                      # show sizes in MB
+alias np='nano -w PKGBUILD'
+alias more=less
 
 xhost +local:root > /dev/null 2>&1
 
@@ -118,10 +118,12 @@
 {
   if [ -f $1 ] ; then
     case $1 in
+      *.tar.xz)     tar xf $1   ;;
+      *.tar.zst)   unzstd $1    ;;
       *.tar.bz2)   tar xjf $1   ;;
       *.tar.gz)    tar xzf $1   ;;
       *.bz2)       bunzip2 $1   ;;
-      *.rar)       unrar x $1     ;;
+      *.rar)       unrar x $1   ;;
       *.gz)        gunzip $1    ;;
       *.tar)       tar xf $1    ;;
       *.tbz2)      tar xjf $1   ;;
@@ -135,3 +137,4 @@
     echo "'$1' is not a valid file"
   fi
 }
+export GPG_TTY=$(tty)
